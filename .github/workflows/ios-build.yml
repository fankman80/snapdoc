name: Build MAUI iOS

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install MAUI Workloads
        run: |
          dotnet workload install maui-ios
          dotnet workload restore

      - name: Select Xcode Version
        run: |
          # Versucht Xcode 16.2 zu nutzen, sonst Standard
          sudo xcode-select -s /Applications/Xcode_16.2.app || sudo xcode-select -s /Applications/Xcode.app
          xcodebuild -version

      - name: Import Certificate (.p12)
        run: |
          # 1. Decode aus Secrets
          echo "${{ secrets.IOS_CERTIFICATE_BASE64 }}" | base64 --decode > certificate.p12
          echo "${{ secrets.IOS_PROVISION_BASE64 }}" | base64 --decode > profile.mobileprovision
          
          # 2. Temporären Schlüsselbund erstellen
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          KEYCHAIN_PASSWORD=$(openssl rand -base64 21)
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          
          # 3. Zertifikat importieren
          security import certificate.p12 -k "$KEYCHAIN_PATH" -P "${{ secrets.IOS_CERT_PASSWORD }}" -A -t cert -f pkcs12
          security list-keychain -d user -s "$KEYCHAIN_PATH"
          
          # 4. Provisioning Profile installieren
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      - name: Build and Sign IPA
        run: |
          dotnet publish SnapDoc/SnapDoc.csproj -f net10.0-ios -c Release \
          -p:ArchiveOnBuild=true \
          -p:CodesignKey="Apple Distribution" \
          -p:CodesignProvision="MAUI_AppStore_Distribution" \
          -p:RuntimeIdentifier=ios-arm64 \
          -p:MtouchNoWarn=7091 \
          -p:SuppressXCodeVersionMismatch=true

      - name: Upload Artifact (Optional zum Download)
        uses: actions/upload-artifact@v4
        with:
          name: SnapDoc-iOS-App-Fixed
          path: SnapDoc/bin/Release/net10.0-ios/ios-arm64/publish/SnapDoc.ipa

      - name: Upload to App Store Connect (TestFlight)
        run: |
          # Lädt die durch 'Add Swift Support' korrigierte Datei zu Apple hoch
          xcrun altool --upload-app \
            -f "SnapDoc/bin/Release/net10.0-ios/ios-arm64/publish/SnapDoc.ipa" \
            -t ios \
            -u "m.fankhauser@gmx.net" \
            -p "${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}"
