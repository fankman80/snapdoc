// SYSTEMVARIABELN FÜR EXPORTVORLAGEN

${pin_table}
// Tabelle mit allen Pins (Platzhalter als Titel in Tabelleneigenschaften)

${client_name}
// Name des Kunden

${object_address}
// Adresse des Objekts

${working_title}
// Arbeitstitel des Projekts

${project_nr}
// Nummer des Projekts

${object_name}
// Objektbezeichnung

${project_manager}
// Projektleiter

${plan_indexes}
// Liste der Pläne

${plan_images/x/y}
// Pläne, x/y = max. Breite und Höhe in Millimeter

${title_image/x/y}
// Titelbild, x/y = max. Breite und Höhe in Millimeter

${pin_nr}
// Fortlaufende Nummer des Pins

${pin_planName}
// Name des Plans

${pin_posImage/x/y}
// Positionsbild (Planausschnitt) mit Icon, x/y = max. Breite und Höhe in Millimeter

${pin_posIcon/x/y}
// Pin-Icon, x/y = max. Breite und Höhe in Millimeter

${pin_fotoList/x/y}
// Die dem Pin zugehörige Fotos, x/y = max. Breite und Höhe in Millimeter

${pin_name}
// Titel oder Bezeichnung des Pins

${pin_desc}
// Beschreibungstext des Pins

${pin_location}
// Standortbeschreibung

${pin_priority}
// Priorität oder Wichtigkeit

${pin_geolocWGS84}
// KGPS-Koordinaten (WGS84)

${pin_geolocCH1903}
// Schweizer Koordinaten (CH1903)

${pin_captureDate}
// Erfassungsdatum des Pins

${creation_date/x}
// Erstellungsdatum Berichts, x = Datumsformat (siehe unten)

${pin_captureTime}
// Erfassungszeit des Pins, x = Datumsformat (siehe unten)

// Datumsformatierung
// d = Tag ohne führende Null (z. B. 5 oder 12)
// dd = Tag immer zweistellig (z. B. 05 oder 12)
// ddd = Wochentag abgekürzt (z. B. Di)
// dddd = Wochentag ausgeschrieben (z. B. Dienstag)
// M = Monat ohne führende Null (z. B. 5 oder 11)
// MM = Monat immer zweistellig (z. B. 05 oder 11)
// MMM = Monatsname abgekürzt (z. B. Mai oder Nov)
// MMMM = Monatsname ausgeschrieben (z. B. Mai oder November)
// yy = Jahr zweistellig (z. B. 26)
// yyyy = Jahr vierstellig (z. B. 2026)