<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/maui/global"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:res="clr-namespace:SnapDoc.Resources.Languages"
               xmlns:services="clr-namespace:SnapDoc.Services"
               BackgroundColor="{AppThemeBinding Light={DynamicResource PrimaryBackground}, Dark={DynamicResource PrimaryBackgroundDark}}"
               HorizontalOptions="Center"
               x:TypeArguments="services:RemoteProjectDto"
               x:Class="SnapDoc.Views.PopupCloudProjects">

    <Grid Padding="12"
          WidthRequest="320"
          RowDefinitions="Auto, *, Auto"
          RowSpacing="10">

        <!-- Header -->
        <Label Grid.Row="0"
               Text="Projekte in der Cloud"
               FontAttributes="Bold"
               HorizontalOptions="Start" />

        <!-- Projektliste -->
        <CollectionView Grid.Row="1"
                        x:Name="ProjectsListView"
                        MaximumHeightRequest="260"
                        SelectionMode="Single"
                        SelectionChanged="OnSelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="services:RemoteProjectDto">
                    <Grid Padding="8"
                          RowDefinitions="Auto, Auto">
                        <Label Grid.Row="0"
                               Text="{Binding FileName}"
                               FontAttributes="Bold"
                               LineBreakMode="TailTruncation" />
                        <Label Grid.Row="1"
                               Text="{Binding LastModified, StringFormat='{0:dd.MM.yyyy HH:mm}'}"
                               FontSize="11"
                               Opacity="0.7" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Buttons -->
        <Grid Grid.Row="2"
              ColumnDefinitions="*, *"
              ColumnSpacing="8">
            <Button Grid.Column="0"
                    Text="{x:Static res:AppResources.abbrechen}"
                    BackgroundColor="{AppThemeBinding Light={DynamicResource Primary}, Dark={DynamicResource PrimaryDark}}"
                    TextColor="{DynamicResource PrimaryDarkText}"
                    Clicked="OnCancelClicked" />
            <Button Grid.Column="1"
                    x:Name="downloadButton"
                    Text="{x:Static res:AppResources.herunterladen}"
                    IsEnabled="False"
                    BackgroundColor="{AppThemeBinding Light={DynamicResource Primary}, Dark={DynamicResource PrimaryDark}}"
                    TextColor="{DynamicResource PrimaryDarkText}"
                    Clicked="OnDownloadClicked" />
        </Grid>
    </Grid>
</toolkit:Popup>