<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/maui/global"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:mr="clr-namespace:MR.Gestures;assembly=MR.Gestures"
             xmlns:m="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"			 
             xmlns:pc="clr-namespace:SnapDoc.ViewModels"
             xmlns:views="clr-namespace:SnapDoc.Views"             
			 BackgroundColor="DimGrey"
             Shell.TitleColor="{DynamicResource PrimaryDark}"
			 x:Class="SnapDoc.Views.ImageViewPage"
             x:Name="ThisPage">

    <Shell.TitleView>
        <Grid ColumnDefinitions="*,Auto,Auto,Auto"
              x:DataType="views:ImageViewPage"
              VerticalOptions="Center">
            <Label Grid.Column="0"
                   Text="{Binding Title, Source={x:Reference Name=ThisPage}}"
                   TextColor="{DynamicResource PrimaryDark}"
                   Background="Transparent"
                   BackgroundColor="Transparent"
                   FontSize="18"
                   VerticalOptions="Center"
                   HorizontalOptions="Start"/>
            <Button Grid.Column="1"
                    IsVisible="{Binding IsGotoPinBtnVisible}"
                    Text="{x:Static m:MaterialOutlined.Recenter}"
                    FontSize="24"
                    Padding="0"
                    TextColor="{DynamicResource PrimaryDark}"
                    Background="Transparent"
                    FontFamily="MaterialOutlined"
                    VerticalOptions="Center"
                    HorizontalOptions="End"
                    ToolTipProperties.Text="Zu Pin wechseln"
                    Clicked="OnEditClicked"/>
            <Button Grid.Column="2"
                    Text="{x:Static m:MaterialOutlined.Fit_screen}"
                    FontSize="24"
                    Padding="0"
                    TextColor="{DynamicResource PrimaryDark}"
                    Background="Transparent"
                    FontFamily="MaterialOutlined"
                    VerticalOptions="Center"
                    HorizontalOptions="End"
                    ToolTipProperties.Text="Foto auf Fenster grösse zoomen"
                    Clicked="ImageFit"/>
            <Button Grid.Column="3"
                    Text="{x:Static m:MaterialOutlined.Delete}"
                    FontSize="24"
                    Padding="0"
                    TextColor="{DynamicResource PrimaryDark}"
                    Background="Transparent"
                    FontFamily="MaterialOutlined"
                    VerticalOptions="Center"
                    HorizontalOptions="End"
                    ToolTipProperties.Text="Foto löschen"
                    Clicked="OnDeleteButtonClicked"/>
        </Grid>
    </Shell.TitleView>

    <AbsoluteLayout x:Name="ImageView" IsClippedToBounds="True" x:DataType="pc:TransformViewModel">
        <mr:ContentView PinchingCommand="{Binding PinchingCommand}"
                        Panning="OnPanning"
                        Pinching="OnPinching"
                        Pinched="OnPinched"
                        DoubleTapped="OnDoubleTapped"
                        ScrollWheelChanged="OnMouseScroll">
            <AbsoluteLayout x:Name="FotoContainer"
				            Scale="{Binding Scale}"
				            TranslationX="{Binding TranslationX}"
				            TranslationY="{Binding TranslationY}"
                            AnchorX="{Binding AnchorX}"
				            AnchorY="{Binding AnchorY}">
                <Image x:Name="FotoImage"/>
            </AbsoluteLayout>
        </mr:ContentView>

        <Button x:Name="DrawBtn" ImageSource="{FontImageSource FontFamily=MaterialOutlined, Glyph={x:Static m:MaterialOutlined.Draw}}"
                BackgroundColor="{DynamicResource Primary}"
                TextColor="{DynamicResource PrimaryDarkText}"
                Margin="0,0,20,20"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30"
                ZIndex="10000"
                AbsoluteLayout.LayoutBounds="1,1,AutoSize,AutoSize"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                Clicked="DrawingClicked"/>

        <AbsoluteLayout x:Name="ToolBtns"
                        IsVisible="False"
                        AbsoluteLayout.LayoutBounds="0, 1, AutoSize, AutoSize"
                        AbsoluteLayout.LayoutFlags="PositionProportional"
                        ZIndex="10000">
            <VerticalStackLayout Spacing="16"
                                 Margin="20,0,0,20"
                                 AbsoluteLayout.LayoutBounds="0, 1, AutoSize, AutoSize"
                                 AbsoluteLayout.LayoutFlags="PositionProportional">
                <Button ImageSource="{FontImageSource FontFamily=MaterialOutlined, Glyph={x:Static m:MaterialOutlined.Ink_eraser}}"
                        BackgroundColor="{DynamicResource Primary}"
                        TextColor="{DynamicResource PrimaryDarkText}"
                        WidthRequest="60"
                        HeightRequest="60"
                        CornerRadius="30"
                        Clicked="EraseClicked"/>
                <AbsoluteLayout>
                    <Button ImageSource="{FontImageSource FontFamily=MaterialOutlined, Glyph={x:Static m:MaterialOutlined.Palette}}"
                                BackgroundColor="{DynamicResource Primary}"
                                TextColor="{DynamicResource PrimaryDarkText}"
                                WidthRequest="60"
                                HeightRequest="60"
                                CornerRadius="30"
                                Clicked="PenSettingsClicked"/>
                    <Border x:DataType="views:ImageViewPage"
                                WidthRequest="22"
                                HeightRequest="22"
                                BackgroundColor="{Binding Source={x:Reference Name=ThisPage}, Path=SelectedColor}"
                                StrokeThickness="0"
                                AbsoluteLayout.LayoutBounds="0, 0, AutoSize, AutoSize"
                                AbsoluteLayout.LayoutFlags="PositionProportional">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="11"/>
                        </Border.StrokeShape>
                        <Border.Shadow>
                            <Shadow Brush="Black" 
                                Opacity="0.3" 
                                Offset="3,3" 
                                Radius="4"/>
                        </Border.Shadow>
                    </Border>
                </AbsoluteLayout>
                <Button ImageSource="{FontImageSource FontFamily=MaterialOutlined, Glyph={x:Static m:MaterialOutlined.Check}}"
                        BackgroundColor="{DynamicResource Primary}"
                        TextColor="{DynamicResource PrimaryDarkText}"
                        WidthRequest="60"
                        HeightRequest="60"
                        CornerRadius="30"
                        Clicked="CheckClicked"/>
            </VerticalStackLayout>
            <HorizontalStackLayout Spacing="16"
                                   Margin="96,0,0,20"
                                   AbsoluteLayout.LayoutBounds="0, 1, AutoSize, AutoSize"
                                   AbsoluteLayout.LayoutFlags="PositionProportional">
                <Button x:Name="DrawPolyBtn"
                        ImageSource="{FontImageSource FontFamily=MaterialOutlined,Glyph={x:Static m:MaterialOutlined.Polyline}}"
                        BackgroundColor="{DynamicResource Primary}"
                        TextColor="{DynamicResource PrimaryDarkText}"
                        WidthRequest="60"
                        HeightRequest="60"
                        CornerRadius="30"
                        Clicked="DrawPolyClicked"/>
                <Button x:Name="DrawFreeBtn"
                        ImageSource="{FontImageSource FontFamily=MaterialOutlined, Glyph={x:Static m:MaterialOutlined.Gesture}}"
                        BackgroundColor="{DynamicResource Primary}"
                        TextColor="{DynamicResource PrimaryDarkText}"
                        WidthRequest="60"
                        HeightRequest="60"
                        CornerRadius="30"
                        Clicked="DrawFreeClicked"/>
            </HorizontalStackLayout>
        </AbsoluteLayout>
    </AbsoluteLayout>
</ContentPage>
